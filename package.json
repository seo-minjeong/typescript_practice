{
  "name": "understanding-ts",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build", 
  	"clean": "tsc --build --clean",
    "start": "lite-server"
  },
  "author": "cw",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.5.5"
  },
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
